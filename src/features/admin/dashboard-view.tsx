import { Card } from '@/components/ui/card';
import { dashboardMetrics, leadSources } from '@/constants/admin';

export function DashboardView() {
  return <div className="grid gap-6"><div className="grid gap-4 md:grid-cols-4">{dashboardMetrics.map((metric) => <Card key={metric.label}><p className="text-sm text-white/45">{metric.label}</p><p className="mt-3 font-display text-3xl text-white">{metric.value}</p><p className="mt-2 text-sm text-emerald-300">{metric.delta} vs mês anterior</p></Card>)}</div><div className="grid gap-6 lg:grid-cols-[1.4fr_.6fr]"><Card><h2 className="font-display text-2xl">Receita estimada e agendamentos</h2><div className="mt-8 flex h-72 items-end gap-3 rounded-3xl border border-white/10 bg-white/[.025] p-6">{[35,54,48,72,66,81,74,92,88,96,90,100].map((height, index) => <div key={index} className="flex-1 rounded-t-xl bg-gold-gradient" style={{ height: `${height}%` }} />)}</div></Card><Card><h2 className="font-display text-2xl">Origem dos leads</h2><div className="mt-6 grid gap-4">{leadSources.map((source) => <div key={source.source}><div className="mb-2 flex justify-between text-sm"><span className="text-white/60">{source.source}</span><span>{source.value}%</span></div><div className="h-2 rounded-full bg-white/10"><div className="h-2 rounded-full bg-omega-gold" style={{ width: `${source.value}%` }} /></div></div>)}</div></Card></div></div>;
}
